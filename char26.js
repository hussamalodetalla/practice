//swap two by two 
// هون حاولت كثير وخاي النسخة تقريبا 70 بالمية وابطة 
function swapTwo(s){
    let s = '';
    for (i= 0; i<s.length ; i++){
        s1 ==s.split().splice(i,i+3).join('');
        if(s1.length==4){
            s1.split().push(charAt(i+2)).join('');
            s1.split().push(charAt(i+3)).join('');
            s1.split().push(charAt(i+1)).join('');
            s1.split().push(charAt(i)).join('');
        }
        else 
        return s1
    }
    return s1;
}